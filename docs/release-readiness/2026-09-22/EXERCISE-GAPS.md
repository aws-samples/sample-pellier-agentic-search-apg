# Governed exercise gap closure

22 September 2026. Follow-up to `SOURCE-AND-GUIDE-AUDIT.md`, based on governed
source `1241f04f865c80403f1b1bbc46a36aa26d67b9b2`. This records source and local
Studio changes. It does not certify AWS deployment, publication or event timing.
Earlier reports describing Jessica stopping at review are superseded by this task
contract, not rewritten as if the new flow had already been rehearsed.

## Findings and changes

1. **Lab 3 inherited token:** both required probes already clear `PELLIER_TOKEN`
   in the child process before using `--user theo`. The parent token remains.
   Studio executes the published commands with a synthetic inherited token.
2. **Execution-path continuity:** the required Lab 3 comparison now identifies
   what Runtime packages and what Lambda implementations it reuses. Lab 1's
   inventory wrapper and Lab 2's planner remain in-process. Managed search does
   not expose the same exclusion/fallback contract. No code-sharing or managed
   fallback parity claim is made. Participants explain this acceptance boundary
   using the package manifest and Lambda signature.
3. **Human decision to effect:** Jessica's required close now runs investigation,
   an explicit return proposal, human confirmation, and governed execution.
   `scripts/prove_operator_review.py` reads the exact review, originating turn,
   action hash, staff principal, execution receipts, audit arguments, write key
   and return ID. Three snapshots distinguish pending, confirmed-without-write,
   and executed-with-one-effect. Failed attempts remain in the report; a later
   successful same-key retry can pass without erasing them. This records a return
   request, not a parcel receipt or refund. The helper never performs an action.
4. **Authored support binding:** the required Lab 3 conversation already asks
   for Theo's ticket history. SQL requires its exact turn, run, customer and
   managed receipt, separately from the direct owned/foreign Gateway probes.
5. **Live fallback:** `scenario=anna-fallback` supplies a disclosed fixed plan to
   the existing comparison endpoint. It bypasses model extraction only, then
   runs the actual embedding, Aurora retrieval, authored fallback and reranking.
   The guide requires an empty strict watch attempt, a recorded `drop_tags`,
   unchanged budget/stock/exclusions, eligible returned IDs and recomputed RRF
   from the exact comparison ID. Normal model-extracted comparison is unchanged.
6. **Coach scope:** root and backend CLAUDE guidance now cover the complete
   governed marker map. Prediction and progressive hints precede scoped edits;
   the participant runs checks, deployment and consequential actions. The coach
   does not read recovery answers or operate the human approval boundary.
7. **Truthful boundaries:** trace depth remains optional. RLS authoring is
   explicitly rollback-only and does not deploy a participant policy. Source
   comments no longer call the published staff credit tool unpublished.

## Local verification

- Backend full run: 3,621 passed, 90 skipped, one recovery-header parity failure;
  the header was synchronized and the affected suite passed afterward. The final
  source CI is the authority for the full published revision.
- Frontend full run: 1,140 passed and one old capstone-copy assertion failed;
  updated assertion and its two-test file passed. Type checking, lint and build pass.
- Studio suite: 131 tests, OK with one optional integration skipped. Tests include
  real disposable PostgreSQL for exact receipt selection, sparse-then-widened
  attempts, retained requirements, ineligible products, wrong review/customer/turn
  and unrelated writes. No workshop AWS state was touched by these checks.
- Required-path budget: 5,397 prose words, 30 Bash blocks, within the unchanged
  5,400/34 limits. Four labs and eight A/B tasks remain. No completion gate was
  moved into optional material to meet that budget.

## Remaining proof boundary

Publish source, verify the remote SHA, and update the local Studio pin together.
Studio commit, push, asset sync and publication remain owner-managed. The final
Studio handoff records that SHA and its CI result.

Run a timed fresh-account rehearsal of the controlled retrieval query, participant
Runtime deployment, Memory extraction, scoped support read, five outcome driver,
Jessica's three-phase review proof and cleanup. In particular, Lab 4's 25-minute
budget remains a target: a local test cannot establish participant pacing or a
successful managed end-to-end workflow. Do not reuse direct CLI write keys as
Operator approval evidence, or treat a source/package match as shared planner proof.
