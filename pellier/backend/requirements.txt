# FastAPI and Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database - PostgreSQL with pgvector
psycopg[binary,pool]>=3.2.0
pgvector>=0.2.5

# AWS SDK
boto3>=1.43.51
botocore>=1.43.51

# Configuration and Environment
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Core Dependencies
numpy>=1.22.0
tenacity>=8.0.0
rich>=13.0.0

# Logging and Utilities
python-json-logger>=3.0.0

# Lab 2: Agents and MCP
strands-agents[otel]
strands-agents-tools
strands-agents-builder
mcp>=1.0.0
httpx>=0.28.1
# Security floor for the MCP/Strands async HTTP dependency.
aiohttp>=3.14.3
defusedxml>=0.7.1
tiktoken>=0.5.0

# Lab 4: AgentCore
PyJWT>=2.8.0
cryptography>=50.0.0
requests>=2.31.0
bedrock-agentcore>=1.4.3

# Caching — Valkey (ElastiCache)
valkey>=6.0.0

# Testing — Exercise 1 check_inventory contract test (test_solutions_parity.py).
# pytest-asyncio is not optional: tests/test_storefront_routes.py drives the
# briefing and pulse routes through `@pytest.mark.asyncio`, and without the
# plugin pytest reports "async def functions are not natively supported" and
# fails all seven. A developer venv usually has it pulled in transitively,
# which hides the gap locally; a clean install has only this file.
pytest>=8.0.0
pytest-asyncio>=0.23.0
